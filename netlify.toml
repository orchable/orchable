[build]
  command = "npm run build"
  publish = "dist"

[context.production]
  environment = { GIT_BRANCH = "stable" }

[context.branch-deploy]
  command = "echo 'Branch deploys disabled'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
